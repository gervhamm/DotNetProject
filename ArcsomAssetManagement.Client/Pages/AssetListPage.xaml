<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:ArcsomAssetManagement.Client.PageModels"
             xmlns:models="clr-namespace:ArcsomAssetManagement.Client.Models"
             xmlns:pages="clr-namespace:ArcsomAssetManagement.Client.Pages"
             xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
             xmlns:controls="clr-namespace:ArcsomAssetManagement.Client.Pages.Controls"
             x:Class="ArcsomAssetManagement.Client.Pages.AssetListPage"
             x:DataType="pageModels:AssetListPageModel"
             xmlns:fonts="clr-namespace:Fonts"
             Title="Assets">
    <ContentPage.Resources>

    </ContentPage.Resources>
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Text="Delete"
            Command="{Binding DeleteCommand}"
            Order="Primary"
            Priority="0"
            IconImageSource="{StaticResource IconDelete}" />
    </ContentPage.ToolbarItems>

    <Grid>
        <ScrollView>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>                    
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Text="Column 1"/>
                <Label Grid.Column="1" Text="Column 2"/>

            </Grid>

        </ScrollView>

        <controls:AddButton Command="{Binding AddAssetCommand}" />
    </Grid>
</ContentPage>