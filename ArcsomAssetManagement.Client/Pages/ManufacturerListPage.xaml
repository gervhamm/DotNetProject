<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:ArcsomAssetManagement.Client.PageModels"
             xmlns:models="clr-namespace:ArcsomAssetManagement.Client.Models"
             xmlns:controls="clr-namespace:ArcsomAssetManagement.Client.Pages.Controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ArcsomAssetManagement.Client.Pages.ManufacturerListPage"
             x:DataType="pageModels:ManufacturerListPageModel"
             Title="Manufacturers">

    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                EventName="Appearing"                
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Sync" Command="{Binding SyncManufacturersCommand}" />
    </ContentPage.ToolbarItems>
    <Grid>
        <ScrollView>
            <VerticalStackLayout 
        Margin="{StaticResource LayoutPadding}" 
        Spacing="{StaticResource LayoutSpacing}">
                <SearchBar Placeholder="Search manufacturers..."
           Text="{Binding SearchText}"
           TextChanged="SearchBar_TextChanged" />

                <Button Text="Search" 
            Command="{Binding FilterManufacturersCommand}"/>
                <VerticalStackLayout 
                    Spacing="{StaticResource LayoutSpacing}"
                    BindableLayout.ItemsSource="{Binding FilteredManufacturers}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="models:Manufacturer">
                            <Border>
                                <VerticalStackLayout>
                                    <Label Text="{Binding Name}" FontSize="18" />
                                    <Label Text="{Binding Contact}" FontSize="14" TextColor="Gray" />
                                </VerticalStackLayout>
                                <Border.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding NavigateToManufacturerCommand, Source={RelativeSource AncestorType={x:Type pageModels:ManufacturerListPageModel}}, x:DataType=pageModels:ManufacturerListPageModel}" CommandParameter="{Binding .}"/>
                                </Border.GestureRecognizers>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>
                <controls:AddButton 
    Command="{Binding AddManufacturerCommand}" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
    
    

</ContentPage>

    